<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.MovieReviewCommentMapper">

	<select id="getList" resultType="org.zerock.domain.MovieReviewCommentVO">
		<![CDATA[
		SELECT * FROM movie_review_comment WHERE mov_rev_com_num > 0
		]]>
	</select>

	<select id="read" resultType="org.zerock.domain.MovieReviewCommentVO">
		<![CDATA[
		SELECT * FROM movie_review_comment WHERE mov_rev_com_num = #{mov_rev_com_num}
		]]>
	</select>

	<insert id="insert" keyProperty="mov_rev_com_num" useGeneratedKeys="true">
		<![CDATA[
		INSERT INTO movie_review_comment(mov_rev_com_content, mov_rev_com_regdate, mov_rev_com_good, mov_rev_com_bad, mem_num, mov_rev_num) 
		VALUES (#{mov_rev_com_content},now(),#{mov_rev_com_good},#{mov_rev_com_bad},#{mem_num},#{mov_rev_num})
		]]>
	</insert>

	<delete id="delete">
		<![CDATA[
		DELETE FROM movie_review_comment WHERE mov_rev_com_num=#{mov_rev_com_num}
		]]>
	</delete>

<!-- 	<update id="update">
		<![CDATA[
		update tbl_board set title =#{title},content=#{content},writer=#{writer},updatedate=now() where bno=#{bno}
		]]>
	</update> -->

	<!-- <sql id="criteria"> <trim prefix="AND (" suffix=")" prefixOverrides="OR"> 
		<foreach item="type" collection="typeArr"> <trim prefix="OR"> <choose> <when 
		test="type == 'T'.toString()"> title like CONCAT('%', #{keyword}, '%') </when> 
		<when test="type == 'C'.toString()"> content like CONCAT('%', #{keyword}, 
		'%') </when> <when test="type == 'W'.toString()"> writer like CONCAT('%', 
		#{keyword}, '%') </when> </choose> </trim> </foreach> </trim> </sql> -->

	<!-- <select id="getTotalCount" resultType="int"> <![CDATA[ SELECT count(*) 
		FROM tbl_board WHERE bno > 0 ]]> <include refid="criteria" /> </select> -->

	<!-- <select id="getListWithPaging" resultType="org.zerock.domain.BoardVO"> 
		<![CDATA[ SELECT * FROM tbl_board WHERE bno > 0 ]]> <include refid="criteria" 
		/> ORDER BY bno DESC LIMIT #{pageStart}, #{amount} </select> -->

	<!-- <insert id="insertSelectKey"> <selectKey order="BEFORE" keyProperty="bno" 
		resultType="Long"> <![CDATA[ select MAX(bno)+1 from tbl_board ]]> </selectKey> 
		<![CDATA[ insert into tbl_board(bno, title, content, writer) values (#{bno},#{title},#{content},#{writer}) 
		]]> </insert> -->



	<!-- <update id="updateReplyCnt"> UPDATE tbl_board SET replycnt=replycnt+#{amount} 
		WHERE bno=#{bno} </update> -->

</mapper>